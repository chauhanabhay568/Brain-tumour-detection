services:
  - type: web
    name: brain-tumor-api
    env: python
    plan: free
    buildCommand: ""
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: MODEL_PATH
        value: bestm.h5
